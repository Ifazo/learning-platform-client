import React from 'react';

const sidenav = (item) => {

    // console.log(item.item.id);

    return (
        <div>
            {/* <p>name: {item.item.name}</p> */}
        </div>
    );
};

export default sidenav;